# Ejecutar
Para correr el primer escenario se tiene que enviar un usuario que no exista
esto en el json "userCreate.json"

# Para ejecutar los test API
./gradlew clean test "-Dkarate.options=--tags @APIDemoblaze"

# Reporte Karate
el reporte se encuentra en la siguiente ruta
"build/karate-reports/karate-summary.html"